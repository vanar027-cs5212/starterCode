#include "Triangle.h"

// bool Triangle::hit(const Ray &r, double ray_tmin, double ray_tmax, HitRecord &rec) const {
//   Point3 p = a + beta * (b - a) + gamma * (c - a);
// }
// Point3 TrianglePoint(float alpha, float beta, float gamma) {
//   return a + beta * (b - a) + gamma * (c - a);
// }
